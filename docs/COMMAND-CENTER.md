# Command Center (Tip 2 / Option 8b)

Stamp **HB-0828.418** / 744. Data plane stays Tip 1 seat packs (`docs/SEAT-SCOPED-PACKS.md`) plus **OM person seats**. Mac and iPad have **no Alerts rail**. Glance tiles use a tight **blue title banner**, a bigger section title, and one **centered** health-tinted SF Symbol — no spark bars. No Dashboard back chevron on iPad/Mac/iPhone scorecards. Mac drops the top **Pages** button — the rail hides in-place and the center fits the window. Phone Pages stays. iPhone Command Center is **PhoneCommandCenterHome**: 1-column scroll of full-width navy heroes + PhoneScorecardRow glance cards — not the iPad leftover-fill grid. Every MetricSection ScoreCard on phone is **PhoneSectionPage** — the same 1-col ScrollView shell, never List + pad `*RollupTable` / `*Table` leftover fill. Compact HubBrandBar is an HStack: Pages must not overlay BeatingHeartbeatMark. iPhone Pages switches keep visited section hosts warm (opacity, not teardown). Filter chips paint cached/last-good chrome immediately, then a cancellable thin pack refresh — same speed bar as page nav. Pull-to-refresh on phone / iPad / Mac Command Center and section pages runs the same no-delete seat-repull as cold open. Who’s looking stays locked off.

Cold open is **company Command Center** (`swapToSeatPack(.company)`). Who's looking is not a required wall. Seat changes are dashboard filter chips only (District / OM / Store / Clear→company). OM chip swaps `packs/seat/om/<slug>/current.sqlite`. No per-page tours. Every filter swap paints or errors — never a silent no-op. **Clear** rewrites the seat row plane in the same turn as chrome (`filteredLatest` / `latestBySection` / roster / expand). Hero Stores N, THIS WEEK, THIS SEAT, and expand are one identity — leftover district 612 under company 2161 is a FAIL. Remount / `filterStamp` / `wipeSeatDashboardState` are not the Clear path.

**Recook + republish required.** Existing device sqlite already baked `operations_om` from OM_AREA (`NorCal 04`…). This tip does not heal packs already on disk.

Home is a **Pulse / Power BI Mobile briefing**: packed KPI tiles, tight gutters, expand on demand. Not always-open ScoreCard tables.

**Heartbeat Assist** (Mac / iPad / iPhone) is an ops coach. Every answer uses **WHAT'S WRONG** (gold pack KPIs for the current seat), **WHAT'S CAUSING IT** (pack facts only), **SHOPPER SOP**, **LABOR / SCHEDULE** (only when those facts exist), and **DIRECTION**. Missing data is stated — never guessed. Done / suggestion chips / send stay ≥ 44pt on phone and comfortable on iPad / Mac.

| Surface | Must |
|---|---|
| Heroes | Sales / Loss / 5 Star navy tiles. Gold **Stores N** is one roster pin (`pinSeatStoreCount`) at company and district. No 2189/2160/2159 split. |
| Glance | Every other **real** `dashboardCards` tile only: **blue title banner** + bigger title + health badge on the banner + value + one **centered** SF Symbol tinted with `displayedHealth`. No spark bars / invented history. Picker headline is pack chrome / `pickerShoppers` (summary-first). Empty cards stay No data — never `.none` → Healthy. |
| Scorecard | Active-filter tables only, never duplicates. **Company:** Regions + Markets. **Region:** Markets. **Division:** Districts + Stores. **District / OM / Store:** Stores once. Picker: **one shoppers table** + individual picture strip on Division / District / OM / Store — **not** Company. Sales: **week total + by day** above the grain rollup on **every** seat. |
| Density | iPad leftover-fill 132/120. **Mac: scroll readable MUST M tiles** (glance floor 196) — do not leftover-fill to `geo.size.height`. **iPhone: scroll.** Do not cram 12 cards into leftover viewport. |
| Expand | Tap opens the scorecard. No store tables on home. |
| Mac | **Collapsible** left Pages rail + scrolling readable center (xxxLarge Dynamic Type + MacReadable metric atoms). No top Pages button. Sidebar-leading icon in rail chrome collapses the list (no floating Hide pages label); center expands; resize reflows heroes / glance / callouts; bottom Prep row is fully visible after scroll (not leftover-filled). Share / New Message is a large resizable pop-out: Back + To above the fold, Send disabled until a valid To, then dismiss → 350ms → MFMailCompose (no Sent toast). Never phone D1–D5 shrink. Picker Top Opportunity keeps column headers (MUST H). **No right Alerts column.** |
| iPad land + port | **Full-width center by default.** Pages opens as a drawer. **No Alerts drawer / rail.** Do not pin Mac triple columns. |
| iPhone | **PhoneCommandCenterHome** (portrait-first): 1-col scroll. Sales / Loss / 5 Star are full-width navy cards (≥100pt, compact chrome). Glance is PhoneScorecardRow list, not a 2-col leftover grid. Content starts below a tighter Pages + Filters + banner stack (subheadline pills, 44pt hits). **Every section page** (Sales, Loss, Missing Items, 5 Star, Pre-Sub, Pick Path, Prep Not Ready, Dynacap, Schedule Quality, Picker, PPH, Labor) is **PhoneSectionPage** — same denser scroll shell. Sales week + By Day are OverviewSalesPhoneCard rows, not OverviewSalesBlock in a List. Compact chrome: Pages only (no Back chevron), beside the mark, never on top of it. iPad leftover-fill stays the existing 132/120 leftover. Mac uses expanded readable chrome (MUST M) — do not apply phone shrink there. Markets/Regions: no Unassigned 21. United Week 27 binds live % — never invent. |
| Load | Heart + progress + chrome. **No grocery one-liners.** |
| Halloween | Off |

Do not merge. Do not TestFlight until Cory PASSes iPad land/port + Mac Catalyst.
